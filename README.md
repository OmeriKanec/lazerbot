## Общая информация
Этот репозиторий содержит **учебный проект по робототехнике**, выполненный на **ROS 2 Jazzy** и связанном программном стеке.

**LazerBot** — это робот на четырёх колёсах с приводом **OmniWheelDriveController**, оснащённый:
- манипулятором (управляется через **MoveIt**);
- RGB‑камерой и **тепловизором**;
- **LiDAR**‑сенсором для восприятия окружающей среды;
- навигационным стеком **Nav2** для автономного перемещения.

---

## ⚙️ Команды запуска

### 1. Сборка проекта
```
colcon build  
source install/setup.bash
```

### 2. Запуск симуляции мира и визуализации
```
ros2 launch lazerbot_launch lazerbot.launch.py
```
(откроется симуляция в Gazebo и **Rviz** с видом с камеры робота)

### 3. Навигация и SLAM
> Запускается **только один** из пунктов ниже, **после** запуска мира:

**3.1 Навигация с SLAM:**
```
ros2 launch lazerbot_launch navigation_with_slam.launch.py
```

**3.2 Навигация по готовой карте:**
```
ros2 launch lazerbot_launch navigation.launch.py
```

**3.3 Создание новой карты (mapping):**
```
ros2 launch lazerbot_launch mapping.launch.py
```

### 4. Управление манипулятором
> Запускается только после запуска мира:
```
ros2 launch lazerbot_launch start_moveit.launch.py
```

---

## 🧩 Структура проекта

- **lazerbot_control/config** — YAML‑файлы конфигураций мостов, навигации и ROS‑контроллеров.  
- **lazerbot_description** — описание модели робота:  
  - `urdf/` — URDF‑файлы робота;  
  - `meshes/` — 3D‑модели деталей;  
  - `maps/` — карты для навигации;  
  - `config/` — файлы настроек RViz;  
  - `worlds/` — Gazebo‑миры.  
- **lazerbot_launch/launch** — все `launch.py` файлы проекта.  
- **lazerbot_moveit_config** — файлы, сгенерированные MoveIt Setup Assistant (частично изменённые).  
  - В `moveit_controllers.yaml` добавлены:
    ```
    action_ns: follow_joint_trajectory
    default: true
    ```
  - В `joint_limits.yaml` заданы ограничения суставов.

---

## 🧱 Трудности при разработке

В процессе работы возникало множество сложностей.

**Примеры:**
- Навигация публиковала команды движения в формате `Twist`, но контроллер не учитывал параметр `use_stamped_vel: false` и ждал `TwistStamped`. Решением стало использование `use_stamped_vel: true` в большей части навигации.
- Контроллер публиковал одометрию в `/omni_wheel_drive_controller/odom`, тогда как навигация ожидала `/odom`.
- Робот поворачивался при езде прямо — проблему в основном удалось устранить, добавив **четвёртое колесо** и уточнив параметры контроллера и модели.
